import Swiper from 'swiper';
import 'swiper/css';

const swiperContainer = document.querySelector('.reviews-container');
console.log(swiperContainer);
async function getReviews() {
    const reviews = 
}
